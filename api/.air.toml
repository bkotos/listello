root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./bin/listello-server ./cmd/listello-server"
  bin = "./bin/listello-server"
  include_ext = ["go"]
  exclude_dir = ["bin", "reports", "tmp", "vendor"]
  exclude_regex = ["_test\\.go"]
  delay = 500
  stop_on_error = true
  send_interrupt = true
  kill_delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
