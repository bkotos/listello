@apiBase = http://localhost:8080
@viteBase = http://localhost:5173

### Health (direct to API)
GET {{apiBase}}/health

### Health (via Vite proxy)
GET {{viteBase}}/health

### Create list (direct to API)
POST {{apiBase}}/api/lists
Content-Type: application/json

{
  "name": "Next actions"
}

### Create list (via Vite proxy)
POST {{viteBase}}/api/lists
Content-Type: application/json

{
  "name": "Next actions"
}
