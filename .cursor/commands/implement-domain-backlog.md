---
name: implement-domain-backlog
description: Implement the next unchecked checkbox in docs/domain-model-backlog.md (one layer, one PR).
---

Follow the project skill **implement-domain-backlog**.

Read `.cursor/skills/implement-domain-backlog/SKILL.md` and `docs/domain-model-backlog.md`. Walk from the top of the backlog, do the first unchecked checkbox only, follow TDD (red → stop for review → green after I approve), mark that checkbox `[x]` in `docs/domain-model-backlog.md` in the same PR, then stop and wait for me to merge before the next layer.
